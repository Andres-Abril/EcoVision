import React, { useState, useEffect } from 'react';
import { SystemLog } from '../types';
import {
  ShieldCheck,
  Terminal,
  Database,
  Lock,
  Key,
  Server,
  Activity,
  CheckCircle2,
  AlertTriangle,
  RefreshCw,
  Cpu,
} from 'lucide-react';

interface SuperAdminViewProps {
  logs: SystemLog[];
}

export const SuperAdminView: React.FC<SuperAdminViewProps> = ({ logs }) => {
  const [logFilter, setLogFilter] = useState<string>('all');
  const [autoRefresh, setAutoRefresh] = useState<boolean>(true);

  const filteredLogs = logs.filter((log) => {
    if (logFilter === 'all') return true;
    return log.service === logFilter || log.level === logFilter;
  });

  return (
    <div className="space-y-8 animate-fade-in pb-12">
      {/* Super Admin Title Banner */}
      <div className="p-6 rounded-3xl bg-slate-950 text-white border border-purple-900/50 shadow-xl flex flex-col md:flex-row items-start md:items-center justify-between gap-6">
        <div className="space-y-1">
          <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-purple-500/20 text-purple-300 text-xs font-semibold border border-purple-500/30">
            <ShieldCheck className="w-3.5 h-3.5" />
            <span>Consola de Super Administración & Auditoría</span>
          </div>
          <h2 className="text-2xl font-bold tracking-tight">Seguridad, Registros & Gobernanza</h2>
          <p className="text-xs text-slate-400">
            Monitor de logs del servidor en tiempo real, auditoría RBAC, políticas JWT y salud de microservicios en la nube.
          </p>
        </div>

        <div className="flex items-center gap-2">
          <span className="w-2.5 h-2.5 rounded-full bg-emerald-500 animate-ping"></span>
          <span className="text-xs font-mono font-bold text-emerald-400">Cloud Cluster Healthy</span>
        </div>
      </div>

      {/* Security Status Cards */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        <div className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-xs">
          <span className="text-xs font-semibold text-slate-500 block mb-1">Cifrado de Sesiones</span>
          <span className="text-lg font-bold text-purple-600 dark:text-purple-400">JWT RS256</span>
          <span className="text-xs text-slate-400 block mt-1">Expiración: 24 Horas</span>
        </div>

        <div className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-xs">
          <span className="text-xs font-semibold text-slate-500 block mb-1">Protección RBAC</span>
          <span className="text-lg font-bold text-emerald-600 dark:text-emerald-400">Activa (4 Roles)</span>
          <span className="text-xs text-slate-400 block mt-1">0 brechas detectadas</span>
        </div>

        <div className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-xs">
          <span className="text-xs font-semibold text-slate-500 block mb-1">Rate Limiting API</span>
          <span className="text-lg font-bold text-blue-600 dark:text-blue-400">100 req / min</span>
          <span className="text-xs text-slate-400 block mt-1">Redis Token Bucket</span>
        </div>

        <div className="p-5 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 shadow-xs">
          <span className="text-xs font-semibold text-slate-500 block mb-1">Cloud Base de Datos</span>
          <span className="text-lg font-bold text-teal-600 dark:text-teal-400">PostgreSQL 16</span>
          <span className="text-xs text-slate-400 block mt-1">Cloud SQL SSL</span>
        </div>
      </div>

      {/* Live System Logs Console */}
      <div className="p-6 rounded-2xl bg-slate-950 border border-slate-800 text-slate-200 space-y-4">
        <div className="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 border-b border-slate-800 pb-4">
          <div className="flex items-center gap-2">
            <Terminal className="w-5 h-5 text-purple-400" />
            <div>
              <h3 className="font-bold text-sm text-white font-mono">/var/log/ecovision-system.log</h3>
              <p className="text-[11px] text-slate-500">Log de eventos de seguridad y motor de inferencia IA</p>
            </div>
          </div>

          <div className="flex items-center gap-3">
            <select
              value={logFilter}
              onChange={(e) => setLogFilter(e.target.value)}
              className="px-3 py-1.5 rounded-xl bg-slate-900 border border-slate-700 text-xs text-slate-200 font-mono focus:outline-hidden"
            >
              <option value="all">Todos los registros</option>
              <option value="IA_ENGINE">IA_ENGINE</option>
              <option value="AUTH">AUTH</option>
              <option value="DATABASE">DATABASE</option>
              <option value="INFO">Nivel: INFO</option>
              <option value="WARN">Nivel: WARN</option>
            </select>
          </div>
        </div>

        {/* Console Box */}
        <div className="font-mono text-xs space-y-2 max-h-96 overflow-y-auto p-2 bg-slate-900/80 rounded-xl border border-slate-800/80">
          {filteredLogs.map((log) => (
            <div key={log.id} className="flex items-start gap-3 hover:bg-slate-800/50 p-1.5 rounded transition">
              <span className="text-slate-500 shrink-0">[{log.timestamp}]</span>
              <span
                className={`font-bold shrink-0 px-1.5 py-0.2 rounded text-[10px] ${
                  log.level === 'INFO' ? 'bg-blue-950 text-blue-400 border border-blue-800' : 'bg-amber-950 text-amber-400 border border-amber-800'
                }`}
              >
                {log.level}
              </span>
              <span className="text-purple-400 font-bold shrink-0">[{log.service}]</span>
              <span className="text-slate-300 flex-1">{log.message}</span>
              <span className="text-slate-600 shrink-0 text-[10px]">{log.ip}</span>
            </div>
          ))}
        </div>
      </div>

      {/* RBAC Security Matrix */}
      <div className="p-6 rounded-2xl bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 space-y-4">
        <h3 className="font-bold text-base text-slate-900 dark:text-white flex items-center gap-2">
          <Lock className="w-5 h-5 text-purple-500" /> Matriz de Permisos RBAC (Role-Based Access Control)
        </h3>

        <div className="overflow-x-auto">
          <table className="w-full text-left text-xs text-slate-700 dark:text-slate-300">
            <thead className="bg-slate-100 dark:bg-slate-800 text-slate-500 uppercase text-[10px] font-bold">
              <tr>
                <th className="p-3">Módulo / Permiso</th>
                <th className="p-3">Ciudadano</th>
                <th className="p-3">Reciclador</th>
                <th className="p-3">Administrador</th>
                <th className="p-3">Super Admin</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-slate-100 dark:divide-slate-800">
              <tr>
                <td className="p-3 font-semibold">Escanear Foto & Inferencia IA</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
              </tr>
              <tr>
                <td className="p-3 font-semibold">Consultar Historial Propio</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
              </tr>
              <tr>
                <td className="p-3 font-semibold">Ver Mapa de Zonas & Puntos de Acopio</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
              </tr>
              <tr>
                <td className="p-3 font-semibold">Registrar Cantidad Recolectada (Kg)</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
              </tr>
              <tr>
                <td className="p-3 font-semibold">Gestionar Usuarios & Cambiar Roles</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
                <td className="p-3 text-emerald-600 font-bold">✓ Sí</td>
              </tr>
              <tr>
                <td className="p-3 font-semibold">Reentrenamiento MLOps & Auditoría de Logs</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-slate-400">✗ No</td>
                <td className="p-3 text-purple-600 font-bold">✓ Exclusivo</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  );
};
